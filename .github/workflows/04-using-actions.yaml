name: 04- Using-actions

on: push

jobs:
  react-app-build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: Printing Folders
        run: ls

